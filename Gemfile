# frozen_string_literal: true

source 'https://rubygems.org'

# Hello! This is where you manage which Jekyll version is used to run.
# When you want to use a different version, change it below, save the
# file and run `bundle install`. Run Jekyll with `bundle exec`, like so:
#
#     bundle exec jekyll serve
#
# This will help ensure the proper Jekyll version is running.
# Happy Jekylling!
gem 'jekyll'
# gem 'github-pages', '~> 213', group: :jekyll_plugins

# If you have any plugins, put them here!
group :jekyll_plugins do
  gem 'bourbon'
  gem 'jekyll-autoprefixer'
  gem 'kramdown-parser-gfm'
  gem 'jekyll-diagrams'
  gem 'jekyll-favicon'
  gem 'jekyll-feed'
  gem 'jekyll-include_sass'
  gem 'jekyll-mermaid'
  gem 'jekyll_picture_tag'
  gem 'jekyll-redirect-from'
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
  gem 'jekyll-timeago'
  gem 'rubocop'
end

# Makes site theme gem
gemspec

# Add the below to plugin group in leu of compiling from source
#   gem 'site-theme', '>= 0.0.1', path: './'

# Required by GitHub pages to avoid alerts
gem 'activesupport', '>= 6.0.3.1'
